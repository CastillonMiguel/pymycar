.. _motorcycle_front_assembly:

Front Assembly
==============

The front assembly is the section where the front wheel is attached, serving as the principal connection between the front wheel and the frame. Also the steer system is directly related to the front assembly. Several configurations and types can be found, with the most common being the fork system, although other setups exist. 

Fork system
-----------

.. note::
    For an interactive visualization of the suspension, view the CAD example representation: :ref:`ref_cad_motorbike_front_assembly_forks`.
