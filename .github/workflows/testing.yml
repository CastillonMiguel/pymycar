name: Unit Testing

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-22.04  

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Conda
      uses: conda-incubator/setup-miniconda@v3
      with:
        python-version: "3.10"
        auto-update-conda: true
        auto-activate-base: false

    - name: Create conda environment from environment.yml
      shell: bash -l {0}
      run: |
        conda env create --file environment.yml  # Create environment from environment.yml
        conda activate pymycar-env  # Replace 'test-env' with your actual environment name

    - name: Install project dependencies (pyproject.toml)
      shell: bash -l {0}
      run: |
        conda activate pymycar-env
        pip install .

    - name: Run tests with pytest
      shell: bash -l {0}
      run: |
        conda activate pymycar-env
        pytest test/ --disable-warnings